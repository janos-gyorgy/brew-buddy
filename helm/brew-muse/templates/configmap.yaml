apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "brew-muse.fullname" . }}-env
  labels:
    {{- include "brew-muse.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

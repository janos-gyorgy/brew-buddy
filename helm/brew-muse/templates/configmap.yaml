apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "brew-muse.fullname" . }}-env
  labels:
    {{- include "brew-muse.labels" . | nindent 4 }}
data:
  API_BACKEND_URL: {{ .Values.env.API_BACKEND_URL | quote }}

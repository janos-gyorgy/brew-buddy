apiVersion: v1
kind: Service
metadata:
  name: {{ include "brew-muse.fullname" . }}
  labels:
    {{- include "brew-muse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "brew-muse.selectorLabels" . | nindent 4 }}
